# GitHub AI attestation workflow

A public OMT-Global reusable GitHub Actions workflow for generating and cryptographically signing AI attestations on commits.

- Uses cosign keyless signing through OIDC.
- Packages attestation behavior for reuse across repositories.
- Connects AI-assisted engineering workflows to verifiable supply-chain evidence.
